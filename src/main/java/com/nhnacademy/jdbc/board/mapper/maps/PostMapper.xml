<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nhnacademy.jdbc.board.mapper.PostMapper">
    <select id="selectPosts" resultType="com.nhnacademy.jdbc.board.domain.post.PostViewVo">
        SELECT post_no    AS postNo
             , title
             , username
             , created_at AS createdAt
             , seq_no     AS sequenceNo
          FROM Post A
               INNER JOIN User B
                       ON B.user_no = A.user_no
         ORDER BY seq_no
    </select>
    <select id="selectPost" resultType="com.nhnacademy.jdbc.board.domain.post.PostDetailViewVo">
        SELECT post_no    AS postNo
             , title
             , content
        FROM Post A
                 INNER JOIN User B
                            ON B.user_no = A.user_no
        where post_no = #{postId}
    </select>

    <select id="selectPost" resultType="com.nhnacademy.jdbc.board.domain.post.Post">
        SELECT post_no   AS postNo
             , title
             , username
             , content
        FROM Post A
             INNER JOIN User B
                     ON B.user_no = A.user_no
        WHERE post_no = #{postNo}
    </select>

    <!-- TODO: 게시판 내용의 목록은 번호, 제목, 작성자, (수정자), 작성일시, 댓글 개수 입니다. -->
    <insert id="insertPost">
        INSERT INTO Post (user_no, parent_post_no, title, content, created_at, post_depth, sequence_no)
        VALUES (#{userNo}, 0, #{title}, #{content}, NOW(), 0, 0)
    </insert>

    <update id="updatePostById">
        UPDATE Post
           SET title = '${title}', content = '${content}'
         WHERE post_no = #{postId}
    </update>

    <delete id="deletePostById">
        DELETE
          FROM Post
         WHERE post_no = #{postId}
    </delete>

</mapper>
